import { Execute } from '../../interfaces/Event';
import { Message } from 'discord.js';

export const name = 'messageCreate';

export const run: Execute = async (client, bot, message: Message) => {};
